# Este arquivo é apenas um exemplo.
# As chaves reais NUNCA devem ser commitadas.
# A aplicação armazena as chaves no LocalStorage do navegador,
# inseridas manualmente na tela de Configurações.
# Estas variáveis existem apenas para eventual uso em build/CI, se necessário.
VITE_BRAPI_TOKEN=
VITE_OPENROUTER_API_KEY=
